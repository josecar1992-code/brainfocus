# Usado por `docker compose build web` para los build args de Vite.
# Copiar a .env en la raíz del repo (no confundir con apps/web/.env, que es
# para `npm run dev` local).
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
VITE_API_URL=https://api.focusbraincr.com
